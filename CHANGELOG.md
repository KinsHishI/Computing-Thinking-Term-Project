# 파일명 자동 생성 기능 개선 완료!

## 🎯 문제 해결

**이전 문제**: 모든 결과가 `last_result.pkl`로 저장되어 새 검색 시 이전 결과가 덮어씌워짐

**해결 방법**: 키워드와 타임스탬프를 포함한 고유한 파일명 자동 생성

## ✅ 개선 사항

### 1. 자동 파일명 생성
```python
# 이전 방식
save_results(data, 'last_result.pkl')  # 항상 같은 이름

# 개선된 방식
save_results(data)  # 자동으로 고유한 이름 생성
# → result_무선마우스_20251227_221828.pkl
```

### 2. 파일명 형식
```
result_[키워드]_[YYYYMMDD_HHMMSS].pkl

예시:
- result_무선마우스_20251227_221828.pkl
- result_USB_메모리_20251227_222015.pkl
- result_키보드마우스_세트_20251227_222143.pkl
```

### 3. 특수문자 자동 처리
- 파일명에 사용할 수 없는 특수문자는 자동으로 제거됩니다
- 공백은 언더스코어(_)로 변환됩니다
- 키워드는 최대 20자로 제한됩니다

```python
# 입력: "키보드/마우스 세트!@#"
# 출력: result_키보드마우스_세트_20251227_222143.pkl
```

## 📦 GUI 버전 개선

### 결과 저장
```python
# 자동으로 고유한 파일명 생성
saved_filename = analyzer.save_results(data)
# "result_노트북_20251227_221828.pkl 파일로 저장되었습니다."
```

### 결과 불러오기
- 파일 선택 대화상자가 열립니다
- 저장된 모든 `.pkl` 파일 중에서 선택 가능
- 각 검색 결과를 개별적으로 관리할 수 있습니다

## 📦 CLI 버전 개선

### 결과 저장
```bash
결과를 저장하시겠습니까? (y/n): y
💾 결과 저장 완료: result_무선마우스_20251227_221828.pkl
```

### 결과 불러오기
저장된 파일 목록이 자동으로 표시됩니다:

```
📂 저장된 결과 파일 목록:
------------------------------------------------------------
 1. result_무선마우스_20251227_222143.pkl      (2025-12-27 22:21)
 2. result_USB_메모리_20251227_222015.pkl      (2025-12-27 22:20)
 3. result_키보드_20251227_221828.pkl          (2025-12-27 22:18)
------------------------------------------------------------

파일 번호 또는 파일명 입력 (Enter=최근 파일): 1
📂 결과 불러오기 완료: result_무선마우스_20251227_222143.pkl
```

## 🔧 코드 수정 내용

### DataAnalyzer 클래스 (두 버전 공통)

```python
@staticmethod
def save_results(data: Dict, filename: str = None):
    """
    분석 결과를 pickle 파일로 저장합니다.
    
    Args:
        data: 저장할 데이터
        filename: 저장할 파일명 (None이면 자동 생성)
    """
    # 파일명이 지정되지 않은 경우 자동 생성
    if filename is None:
        from datetime import datetime
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        keyword = data.get('keyword', 'unknown')
        # 파일명에 사용할 수 없는 문자 제거
        safe_keyword = "".join(c for c in keyword if c.isalnum() or c in (' ', '_')).strip()
        safe_keyword = safe_keyword.replace(' ', '_')[:20]
        filename = f'result_{safe_keyword}_{timestamp}.pkl'
    
    with open(filename, 'wb') as f:
        pickle.dump(data, f)
    
    return filename  # 저장된 파일명 반환
```

### CLI 버전 - 파일 목록 표시

```python
# 저장된 pkl 파일 목록 표시
import glob
pkl_files = sorted(glob.glob('*.pkl'), key=os.path.getmtime, reverse=True)

if not pkl_files:
    print("\n⚠️  저장된 결과 파일이 없습니다.")
else:
    print("\n📂 저장된 결과 파일 목록:")
    for i, file in enumerate(pkl_files[:10], 1):
        mtime = os.path.getmtime(file)
        date_str = datetime.fromtimestamp(mtime).strftime("%Y-%m-%d %H:%M")
        print(f"{i:2d}. {file:40s} ({date_str})")
```

### GUI 버전 - 파일 선택 대화상자

```python
from tkinter import filedialog

# pkl 파일 선택 대화상자
filename = filedialog.askopenfilename(
    title="결과 파일 선택",
    initialdir=os.getcwd(),
    filetypes=[("Pickle files", "*.pkl"), ("All files", "*.*")]
)
```

## 💡 사용 예시

### 여러 상품을 순차적으로 분석

```bash
# CLI 버전으로 여러 키워드 분석
python3 price_analyzer_cli.py 무선마우스
python3 price_analyzer_cli.py 무선키보드
python3 price_analyzer_cli.py "USB 메모리"

# 생성되는 파일들
# result_무선마우스_20251227_221828.pkl
# result_무선키보드_20251227_222015.pkl
# result_USB_메모리_20251227_222143.pkl
```

### 이전 결과와 비교

```bash
# 1. 어제 저장한 결과
result_노트북_20251226_153022.pkl

# 2. 오늘 저장한 결과
result_노트북_20251227_221828.pkl

# → 두 파일을 비교하여 가격 변동 확인 가능
```

## 📊 장점

1. ✅ **덮어쓰기 방지**: 각 검색 결과가 별도 파일로 저장
2. ✅ **쉬운 식별**: 파일명만 봐도 내용을 알 수 있음
3. ✅ **시간 추적**: 타임스탬프로 언제 수집했는지 확인
4. ✅ **비교 분석**: 여러 키워드나 시간대 비교 가능
5. ✅ **안전한 관리**: 중요한 결과를 잃어버릴 위험 없음

## 🧪 테스트

테스트 스크립트가 포함되어 있습니다:

```bash
python3 test_filename.py
```

테스트 결과:
- ✅ 자동 파일명 생성
- ✅ 특수문자 처리
- ✅ 중복 방지
- ✅ 파일 불러오기

## 🗑️ 파일 관리

### 오래된 파일 정리

```bash
# 7일 이상 된 결과 파일 삭제
find . -name "result_*.pkl" -mtime +7 -delete

# 특정 키워드의 결과만 삭제
rm result_무선마우스_*.pkl

# 모든 결과 파일 삭제 (주의!)
rm result_*.pkl
```

### 파일 확인

```bash
# 모든 결과 파일 목록
ls -lht result_*.pkl

# 최근 10개 파일
ls -lt result_*.pkl | head -10

# 파일 개수 확인
ls result_*.pkl | wc -l
```

## 📝 업데이트 내용

### 수정된 파일
1. `price_analyzer.py` (GUI 버전)
   - `DataAnalyzer.save_results()` 메서드 개선
   - `PriceAnalyzerGUI.save_results()` 메서드 업데이트
   - `PriceAnalyzerGUI.load_results()` 파일 선택 대화상자 추가

2. `price_analyzer_cli.py` (CLI 버전)
   - `DataAnalyzer.save_results()` 메서드 개선
   - `interactive_mode()` 파일 목록 표시 기능 추가

3. `.gitignore`
   - `result_*.pkl` 패턴 추가

4. `test_filename.py`
   - 새로운 기능 테스트 스크립트 추가

5. `README.md`
   - 파일명 자동 생성 기능 설명 추가

## ✨ 결론

이제 **모든 검색 결과가 고유한 파일명으로 자동 저장**되어, 이전 결과를 잃어버릴 걱정 없이 여러 상품을 분석할 수 있습니다!

---

**업데이트 날짜**: 2025년 12월 27일  
**버전**: v1.1 (파일명 자동 생성 기능 추가)
